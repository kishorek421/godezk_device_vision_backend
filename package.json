{
  "name": "godezk-telemetry-dashboard-backend",
  "version": "1.0.0",
  "description": "Telemetry Dashboard Backend — pulls execution and pipeline timing data from the GoDezk device backdoor backend and exposes list + analytics APIs.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "private": true,
  "dependencies": {
    "axios": "^1.14.0",
    "cors": "^2.8.6",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "redis": "^4.6.10"
  }
}
